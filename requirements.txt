# Runtime dependencies (pinned for reproducible builds).
# The authoritative source is pyproject.toml [project.dependencies];
# this file exists so `pip install -r requirements.txt` works in plain Docker/CI stages.
fastapi==0.116.2
uvicorn[standard]==0.36.0
pydantic==2.11.9
pydantic-settings==2.13.1
python-dotenv==1.1.1
python-multipart==0.0.20
requests==2.32.5
