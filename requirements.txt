# Runtime dependencies (pinned for reproducible builds).
# The authoritative source is pyproject.toml [project.dependencies];
# this file exists so `pip install -r requirements.txt` works in plain Docker/CI stages.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
python-multipart==0.0.32
requests==2.34.2
sqlalchemy
pymysql
cryptography

# ML dependencies
scikit-learn>=1.7.2
joblib>=1.6.0
pandas>=2.3.3
numpy>=2.2.6

# Optional: Advanced ML libraries (uncomment for enhanced models)
# xgboost>=2.0.0
# lightgbm>=4.0.0
